// Matches Python's f"${value:,.2f}" formatting used in app/dashboard.py.
export function formatMoney(value: number): string {
  return `$${value.toLocaleString("en-US", {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2,
  })}`;
}
